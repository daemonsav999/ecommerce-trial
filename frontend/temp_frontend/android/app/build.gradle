plugins {
    id("com.android.application")
    id("org.jetbrains.kotlin.android")
    id("com.facebook.react")
}

android {
    namespace = "com.daemonsav999.ecommercebeta2"
    compileSdk = rootProject.ext.compileSdkVersion

    defaultConfig {
        applicationId = "com.daemonsav999.ecommercebeta2"
        minSdk = rootProject.ext.minSdkVersion
        targetSdk = rootProject.ext.targetSdkVersion
        versionCode = 1
        versionName = "1.0.0"
    }

    buildTypes {
        release {
            minifyEnabled = false
            proguardFiles(
                getDefaultProguardFile("proguard-android-optimize.txt"),
                "proguard-rules.pro"
            )
        }
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }
}

dependencies {
    implementation("com.facebook.react:react-android")
    implementation("com.facebook.react:hermes-android")

    debugImplementation("com.facebook.flipper:flipper:${findProperty('FLIPPER_VERSION') ?: '0.201.0'}")
    debugImplementation("com.facebook.flipper:flipper-network-plugin:${findProperty('FLIPPER_VERSION') ?: '0.201.0'}")
    debugImplementation("com.facebook.flipper:flipper-fresco-plugin:${findProperty('FLIPPER_VERSION') ?: '0.201.0'}")

    implementation("androidx.core:core-ktx:${rootProject.ext.androidXCore}")
    implementation("androidx.appcompat:appcompat:${rootProject.ext.androidXAppCompat}")
    implementation("androidx.swiperefreshlayout:swiperefreshlayout:${rootProject.ext.androidXSwipeRefreshLayout}")
}

apply from: project(':react-native').projectDir.getPath() + '/react.gradle'
